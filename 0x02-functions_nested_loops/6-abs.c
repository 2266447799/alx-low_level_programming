#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: character to be checked
 *
 * Return: return integer
 */

int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
